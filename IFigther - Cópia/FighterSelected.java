import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class FighterSelected here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class FighterSelected extends World
{

    /**
     * Constructor for objects of class FighterSelected.
     * 
     */
    public FighterSelected()
    {    
        // Create a new world with 600x400 cells with a cell size of 1x1 pixels.
        super(800, 400, 1); 
        addObject(new Neg‹oSelect(), 622, 218); 
        addObject(new HenriqueSelect(), 390, 207); 
        addObject(new VS(), 406, 201); 
    }
}
